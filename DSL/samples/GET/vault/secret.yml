# Protected by the sibling `.guard.yml` file above. GET /samples/vault/secret
# needs `X-Vault-Token: <anything>` or the guard returns 401.

# h2ck.me v1 T-12 — declaration block added to demonstrate
# the declaration surface. `additive: true` means the
# allowlist below is documentation-only; undeclared fields
# still pass through, so this sample keeps its existing
# runtime behaviour. Set `strict: true` or remove `additive`
# to enforce.
declaration:
  description: "Protected by the sibling `.guard.yml` file above. GET /samples/vault/secret"
  additive: true

respond:
  return: { secret: "42" }
  next: end
