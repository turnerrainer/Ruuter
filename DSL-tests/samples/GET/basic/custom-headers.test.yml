mode: inprocess

tests:
  - name: sets custom response headers
    request: { method: GET, path: /samples/basic/custom-headers }
    expect:
      status: 200
      body: { response: { message: "Response with custom headers" } }
      headers:
        X-Custom-Header: "Custom Value"
        X-API-Version: "1.0"
